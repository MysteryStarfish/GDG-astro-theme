---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';

interface Props {
    title: string;     
    date: string;        
    image?: ImageMetadata;        
    url: string;
}
const { 
    title, 
    date, 
    image,
    url
} = Astro.props;
---
<a href={url} class={`group relative block h-53 overflow-hidden rounded-[30px]`}>
    {image && <Image 
        src={ image } 
        alt={ title } 
        class="group-hover:scale-110 group-hover:opacity-50 object-cover absolute inset-0 w-full h-full transition duration-700"/>
    }
    
    <div class="from-[#444444] to-[#7e7d7d44] bg-linear-to-t absolute inset-0"></div>

    <div class="absolute bottom-0 p-6">
        <div class="mb-1 text-sm text-[#e4e4e4]">{date}</div> 
        <div class="text-xl font-bold text-white">
            {title}
        </div>
    </div>
</a>